# Copy to /etc/canary.env on the VPS (and source locally with the SAME key).
# Generate once:  openssl rand -hex 32
CANARY_HMAC_KEY=REPLACE_ME_WITH_openssl_rand_hex_32
CANARY_LOG_PATH=/var/lib/canary/hits.jsonl
